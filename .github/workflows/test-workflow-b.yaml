name: Workflow B
on:
  workflow_run:
    workflows: ["Workflow A"]
    types:
      - completed
jobs:
  workflow-a-status:
    if: ${{ github.event.workflow_run.conclusion == 'failure' }}
    runs-on: 'ubuntu-latest'
    steps:
    - env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        gh api \
          --method POST \
          -H "Accept: application/vnd.github+json" \
          /repos/${{ github.repository }}/actions/runs/${{ github.event.workflow.id }}/rerun
